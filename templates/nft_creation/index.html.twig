{# {% extends 'base.html.twig' %}

{% block title %}NFT Creation{% endblock %}

{% block body %}

<h1 class="text-center m-5">Ma Création</h1>
{{ form_start(form) }}
    {{ form_row(form.valeur_euro) }}
    {{ form_row(form.prix_eth) }}
    {{ form_row(form.is_en_vente) }}
    <h2>{{ form_row(form.categories) }}</h2>
        
        
        
    {{ form_row(form.image) }}

    <button type="submit" class="btn btn-primary mb-5">Creation</button>
{{ form_end(form) }}
{% endblock %} #}

{% extends 'base.html.twig' %}

{% block title %}Gestion NFT
{% endblock %}

{% block body %}
<div class="container w-75 mx-auto my-5">
	{{form_start(form)}}
    {{form_rest(form)}}
        {% if nft.id is same as(null) %}
        <button type="submit" class="btn btnViolet">Créer</button>
    {% else %}
        <button type="submit" class="btn btnViolet">Modifier</button>
    {% endif %}
	{{form_end(form)}}
</div>

{% endblock %}

{% block javascripts %}
	{{ parent() }}
	<script src="https://cdn.jsdelivr.net/npm/tom-select@2.2.2/dist/js/tom-select.complete.min.js"></script>
	<script defer src="{{asset('/js/select.js')}}"></script>
{% endblock %}
